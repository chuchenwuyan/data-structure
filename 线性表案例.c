//线性表：一排有限的、同类型的数据元素，有先后位置关系。
// “线性”表示元素像队伍一样排列，不表示必须按大小排序。
//
// 元素可以是 int，也可以是 Book 结构体，也可以是多项式的 Term 结构体。
//
// 线性表是逻辑结构，不等于数组。
// 用数组实现线性表叫顺序表；用结点和指针实现线性表叫链表。
//
// 线性表常见操作：初始化、取值、查找、插入、删除、遍历、求长度。