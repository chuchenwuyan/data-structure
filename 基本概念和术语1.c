//数据由数据元素构成，由数据项构成。
//这样看可能就是一个嵌套结构体？嵌套了两层。        或者说是一个DateFrame

//数据元素之间不是孤立的，他们之间的关系叫做结构

//为什么在线性结构要提到“栈”？ 
//  在C的学习中，我理解的栈是一个存储空间，随着函数的运行，会在栈中增加占用空间的数据，也会有出去的数据，没记错的话是后进先出

//学习到现在，老师一直灌输的都是一些专有名词的介绍，并没有代码上的直接学习

//突然由一个想法，字符串，直接看就像是多个字符被串在了一起。而不是像"abcd"这样的，
// 但如果是多个字符串在一起，那么这就该叫字符串数组了

//链式结构的存储时用一组任意存储单元。  
// 那么如果是一组的数据需要存储，比如[eat,cat]这样先存储的不只是eat，还有cat的地址？为什么要这样